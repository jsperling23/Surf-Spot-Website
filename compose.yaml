services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.backend
    env_file:
      - ./backend/.env
    image: surf-backend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.frontend
    image: surf-frontend
    ports:
      - 3000:80




